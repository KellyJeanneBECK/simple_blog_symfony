<nav>
    <h1><a href="{{ path('app_home') }}">Simple Blog</a></h1>
    <ul>
        {% if app.user %}
            <li><a href="{{ path('app_article') }}">My articles</a></li>
            <li><a href="{{ path('app_article_new') }}">New article</a></li>
        {% endif %}
        {% if is_granted("ROLE_ADMIN") %}
            <li><a href="{{ path('app_category_manager') }}">Category Manager</a></li>
        {% endif %}
    </ul>
    <ul>
        {% if app.user %}
            <li><a href="{{ path('app_logout') }}">Logout</a></li>
        {% else %}
            <li><a href="{{ path('app_register') }}">Register</a></li>
            <li><a href="{{ path('app_login') }}">Login</a></li>
        {% endif %}
    </ul>
</nav>